{
  "name": "itadis-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"import('fs').then(f=>f.copyFileSync('public/_headers','dist/_headers'))\"",
    "preview": "vite preview",
    "optimize-images": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^8.2.0"
  }
}
